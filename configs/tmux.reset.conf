# Additions on top of standard tmux defaults (nothing removed or swapped)

# Vim-style pane navigation (arrow keys still work too)
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Intuitive splits that preserve current path (% and " still work too)
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Vi copy mode selection
bind-key -T copy-mode-vi v send-keys -X begin-selection

# Reload config
bind R source-file ~/.config/tmux/tmux.conf \; display "Config reloaded"
